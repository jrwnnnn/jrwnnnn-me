---
import "../../styles/marked.css";
// import { createClient } from "@libsql/client";
// import { createSlug } from "@utils/createSlug";

import Layout from "../../layouts/Layout.astro";

// ILL BE IMPLEMENTING THIS SOON, JUST WANT A PLACEHOLDER FOR NOW
// const turso = createClient({
// 	url: process.env.TURSO_DATABASE_URL || import.meta.env.TURSO_DATABASE_URL,

// 	authToken: process.env.TURSO_AUTH_TOKEN || import.meta.env.TURSO_AUTH_TOKEN,
// });

// let message = "";

// if (Astro.request.method === "POST") {
// 	try {
// 		const data = await Astro.request.formData();
// 		const title = data.get("title")?.toString() ?? "";
// 		const subtitle = data.get("subtitle")?.toString() ?? null;
// 		const content = data.get("content")?.toString() ?? "";
// 		// TODO: Prevent duplicate slugs by appending a number if the slug already exists
// 		const slug = createSlug(title);

// 		if (title) {
// 			await turso.execute(
// 				"INSERT INTO articles (title, subtitle, content, date_created, slug) VALUES (?, ?, ?, ?, ?)",
// 				[title, subtitle, content, new Date().toISOString(), slug],
// 			);
// 			message = "Record inserted successfully.";
// 			return new Response(null, {
// 				status: 301,
// 				headers: { Location: `/transmission/${slug}` }
// 			});
// 		}
// 	} catch (error) {
// 		message = `Insertion failed: ${error instanceof Error ? error.message : "Unknown error"}`;
// 	}
// }
---

<Layout title="Mark Jerwin // TRANSMIT" robots="noindex, nofollow">
	<main class="flex flex-col items-center justify-center">
		<svg
			id="preview-toggle"
			xmlns="http://www.w3.org/2000/svg"
			viewBox="0 0 24 24"
			fill="#000"
			stroke="#00c951"
			width="30"
			height="30"
			class="mb-10 cursor-pointer"
		>
			<path
				d="M12 4.5C7 4.5 2.73 7.61 1 12c1.73 4.39 6 7.5 11 7.5s9.27-3.11 11-7.5c-1.73-4.39-6-7.5-11-7.5zM12 17c-2.76 0-5-2.24-5-5s2.24-5 5-5 5 2.24 5 5-2.24 5-5 5zm0-8c-1.66 0-3 1.34-3 3s1.34 3 3 3 3-1.34 3-3-1.34-3-3-3z"
			></path>
		</svg>

		<div class="w-full font-mono md:w-[60vw]">
			<statusbar
				class="flex items-center justify-between border border-b-0 border-green-800 bg-green-900/20 px-4 py-2 text-xs tracking-widest uppercase"
			>
				<span>PREVIEW</span>

				<time datetime={new Date().toISOString()}>
					{
						new Date().toLocaleDateString("en-US", {
							year: "numeric",
							month: "short",
							day: "numeric",
						})
					}
				</time>
			</statusbar>

			<form
				method="POST"
				class="flex flex-col space-y-5 border border-green-800 bg-black p-6 md:p-12"
			>
				<textarea
					class="w-full rounded-none text-4xl leading-none font-bold uppercase outline-none md:text-5xl"
					name="title"
					placeholder="Title"
					rows="1"
					oninput="this.style.height = ''; this.style.height = this.scrollHeight + 'px'"
					required></textarea>

				<textarea
					class="w-full text-lg text-green-600 outline-none"
					name="subtitle"
					placeholder="Subtitle"
					rows="2"
					oninput="this.style.height = ''; this.style.height = this.scrollHeight + 'px'"
				></textarea>

				<hr class="border border-green-900/50" />

				<textarea
					id="content-input"
					class="h-full min-h-30 w-full resize-none overflow-hidden outline-none"
					name="content"
					placeholder="Start typing here..."
					oninput="this.style.height = ''; this.style.height = this.scrollHeight + 'px'"
					required></textarea>

				<div id="preview" class="prose hidden min-h-30"></div>

				<hr class="border border-green-900/50" />

				<p class="text-center text-sm text-green-800 italic">
					END OF TRANSMISSION
				</p>

				<!-- <button
					type="submit"
					class="cursor-pointer self-end px-3 py-2 text-sm font-bold tracking-widest hover:bg-green-500 hover:text-black hover:brightness-110"
				>
					[ TRANSMIT ]
				</button> -->
			</form>
		</div>
	</main>
</Layout>

<script>
	import { marked } from "marked";
	const toggle = document.getElementById("preview-toggle");
	const input = document.getElementById("content-input") as HTMLTextAreaElement;
	const preview = document.getElementById("preview");

	if (toggle && input && preview) {
		toggle.addEventListener("click", async () => {
			const isEditing = !input.classList.contains("hidden");

			if (isEditing) preview.innerHTML = await marked.parse(input.value);
			input.classList.toggle("hidden", isEditing);
			preview.classList.toggle("hidden", !isEditing);
			toggle.setAttribute("fill", isEditing ? "#00c951" : "#000");
			toggle.setAttribute("stroke", isEditing ? "#000" : "#00c951");
		});
	}
</script>
